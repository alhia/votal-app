

class FileApi {

/*   Future<void> create({required File file}) async {
    _log.i('File:$File');

    try {
      final fileDocument = await _storage.ref();
      _log.v('FileCreated at ${fileDocument}');
    } catch (error) {
      throw FirestoreApiException(
        message: 'Failed to create new File',
        devDetails: '$error',
      );
    }
  } */
}
